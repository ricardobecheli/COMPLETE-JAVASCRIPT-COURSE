// note that Math starts with CAPITAL M!

const radius = 5.6
const area = Math.PI * Math.pow(radius, 2)

console.log(area)
console.log(typeof Math)

        // Math IS AN OBJECT.

        // It has a constant called PI

        // And one of its functions is "pow", to calculate powers in the shape (base, exponent) as row 4 above: (radius, 2).

